org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# El daemon de Kotlin 2.3.20 registra dos veces las cachés incrementales de los módulos de los
# plugins (share_plus, image_picker_android, shared_preferences_android) y revienta al cerrarlas.
# Esos módulos son dependencias de terceros que no se editan, así que no perdemos nada útil.
kotlin.incremental=false
